extends layout 

block content
    if errorMessage    
        span=errorMessage
    form(method="POST")
        span 가입에 사용된 정보를 입력해주세요.
        input(type="text" name="name" placeholder="이름" required)
        input(type="email" name="email" placeholder="이메일" required)
        input(type="submit", value="아이디 찾기")
    br
    if findMessage 
        span=findMessage
    br
    a(href="/login") 로그인 